<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Тестовое задание</title>
</head>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: monospace;


        --dark: hsl(242, 35%, 17%);
        --light: hsl(209, 60%, 75%);
        --red: hsl(0, 50%, 52%);
        --green: hsl(115, 49%, 52%);
    }

    a {
        text-decoration: none;
        text-align: center;
    }

    .wrapper {
        width: 100vw;
        height: 100vh;
        overflow: hidden;
        background-color: var(--light);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    form,
    .info {
        width: 50rem;
        height: 30rem;
        background-color: white;
        display: flex;
        align-items: center;
        flex-direction: column;
        justify-content: center;
        border-radius: 2rem;
        box-shadow: 0.4rem 0.4rem 0 0 var(--dark);
    }

    label {
        text-decoration: underline;
        text-transform: uppercase;
        font-weight: 700;
        font-size: 2rem;
        margin: .8rem;
        cursor: pointer;
    }

    input[type="text"] {
        padding: .5rem;
        outline: none;
        border: .2rem solid var(--dark);
        opacity: .6;
        border-radius: .5rem;
        font-size: 2rem;
        transition: opacity .4s;
        margin-bottom: .8rem;
    }

    input[type="text"]:hover,
    input[type="text"]:focus-visible,
    input[type="text"]:focus {
        opacity: .8;
    }

    input[type="submit"],
    a {
        position: relative;
        width: 12rem;
        padding: 1rem 2rem;
        margin: 2rem 0;
        border-radius: 0.2rem;
        transition: box-shadow 0.2s, top 0.2s, left 0.2s;
        border: 0.1rem solid var(--dark);
        cursor: pointer;
        font-weight: 600;
        background-color: white;
        color: var(--dark);

        box-shadow: .2rem .2rem 0px 0px var(--dark);
        top: 0rem;
        left: 0rem;
    }

    input[type="submit"]:hover,
    a:hover {
        top: 0rem;
        left: 0rem;
        box-shadow: 0.2rem 0.2rem 0px 0px var(--light);
    }

    input[type="submit"]:active {
        top: 0.2rem;
        left: 0.2rem;
        box-shadow: 0rem 0rem 0px 0px var(--dark);
    }

    .hint {
        width: 100%;
        height: 3rem;
        text-align: center;
    }

    .hint__content.false {
        color: var(--red);
    }

    .hint__content.true {
        color: var(--green);
    }
</style>

<body>
    <div class="wrapper">
        <% if (!check) { %>
            <form action="/" method="post">
                <label for="name">Имя пользователя</label>
                <input type="text" id="name" name="user" required>
                <input type="submit" value="Отправить" />
            </form>
            <% } else { %>
                <div class="info">
                    <div class="hint">
                        <h1 class="hint__content <%- has %>">
                            <%= name %> ->
                                <%= has==="true" ? 'Пользователь найден' : 'Пользователь не найден' %>
                        </h1>
                    </div>
                    <a href="/">Назад</a>
                </div>
                <% } %>
    </div>
</body>

</html>